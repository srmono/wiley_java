package Patterns;

import java.util.regex.Pattern;

public class RegexBasics {
	public static void main(String[] args) {
		Pattern pattern = Pettern.compile("");
		Mather matcher = pattern.matcher(pattern);
	}
}
